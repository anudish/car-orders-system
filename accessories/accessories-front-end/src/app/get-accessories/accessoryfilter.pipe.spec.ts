import { AccessoryfilterPipe } from './accessoryfilter.pipe';

describe('AccessoryfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AccessoryfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
