import { OrdersfilterPipe } from './ordersfilter.pipe';

describe('OrdersfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdersfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
